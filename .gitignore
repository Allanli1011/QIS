# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# 行情缓存与回测输出（可再生成，不入库）
# 必须用 / 锚定到仓库根：不带斜杠的 data/ 会匹配任意层级的同名目录，
# 把整个 src/qis/data/ 数据层源码也一并忽略掉。
/data/
/reports/

# pytest 缓存
.pytest_cache/
